# Content Rendering Architecture

This document moved to [`docs/content-pipeline.md`](../../../docs/content-pipeline.md),
which covers the full pipeline: fetching/caching in `github.ts`, the renderer
registry, the markdown renderer's extensions, wiki-link resolution, and how to add
a new format.

See [`docs/README.md`](../../../docs/README.md) for the full documentation index.
